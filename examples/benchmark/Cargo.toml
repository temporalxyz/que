[package]
name = "benchmark"
version = "0.1.0"
edition = "2021"

[dependencies]
bytemuck = { workspace = true, features = ["derive", "min_const_generics"] }
nix = { workspace = true }
que = { workspace = true }
